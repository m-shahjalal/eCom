{
  "buildCommand": "cd client && set NODE_OPTIONS=--openssl-legacy-provider && npm run build",
  "outputDirectory": "client/build",
  "framework": "create-react-app",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
